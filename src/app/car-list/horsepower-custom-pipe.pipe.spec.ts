import { HorsepowerCustomPipePipe } from './horsepower-custom-pipe.pipe';

describe('HorsepowerCustomPipePipe', () => {
  it('create an instance', () => {
    const pipe = new HorsepowerCustomPipePipe();
    expect(pipe).toBeTruthy();
  });
});
